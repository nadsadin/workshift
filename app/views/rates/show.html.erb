<p id="notice"><%= notice %></p>

<p>
  <strong>Rate:</strong>
  <%= @rate.rate %>
</p>
<%= link_to 'New', new_rate_path, class: 'btn btn-primary' %> | 
<%= link_to 'Edit', edit_rate_path(@rate), class: 'btn btn-primary' %> |
<%= link_to 'Back', rates_path, class: 'btn btn-primary' %>
